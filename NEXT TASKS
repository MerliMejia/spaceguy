Animation And Time Playback System

Time:

[] Have a place where the time logic exists. Things like delta time (using std::kronos).


Animations

[] It should store the animation states used for all the animation calculations:
    - playbackSpeed

[] It should use the logic for time.

[] Have a function (maybe something that generates these:

```
.previousPositionOffset = previousPose.positionOffset,
                .nextPositionOffset = nextPose.positionOffset,
                .interpolation = interpolation,
```

) that can be called getAnimationDataFromObject(3DObject). It should calculate the blender frame float, use it to interpolate between frames and generate the data.